@model IEnumerable<Repository.Models.CVApplication>

@{
    ViewBag.Title = "Lista Aplikacji - metaTitle - do 60 znaków";
    ViewBag.Opis = "Lista złożonych CV";
    ViewBag.SlowaKluczowe = "Lista aplikacji, złożone CV";
}

<h2>Aplikacje</h2>

<p>
    @Html.ActionLink("Dodaj", "Create", null, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Workplace)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataDodania)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CVFileId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Workplace)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataDodania)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CVFileId)
            @Html.ActionLink("Pobierz", "DownloadCVFile", new { id = 3 });
        </td>
        <td>
            @Html.ActionLink("Szczegóły", "Details", new { id = item.Id }, new { @class = "btn btn-primary" }) |
            @*@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))*@
            @if (User.Identity.IsAuthenticated)
                {

                @Html.ActionLink("Edytuj", "Edit", new { id = item.Id }, new { @class = "btn btn-warning" })

                @: |
                @Html.ActionLink("Usuń", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
            }
        </td>
    </tr>
}

</table>
